// Appearance (light / dark / system) — applies `body.dark` from
// settings.appearance and the OS preference. Implemented by the dark-mode
// work; wired from main.tsx.
import type { AppStore } from './store';

export function installAppearance(_store: AppStore): void {
  /* filled in by the dark-mode implementation */
}
